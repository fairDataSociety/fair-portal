import { createTheme } from "@mui/material";

const darkTheme = createTheme({
  palette: {
    border: {
      main: "#ddd",
    },
    link: {
      main: "#333",
      dark: "#888",
    },
  },
});

export default darkTheme;
